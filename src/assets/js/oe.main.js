(function(window, $) {
  var $doc = $(document);
  var $body = $('body');
  var $grand = $('body, html');

  var config = {
  }

  // Common functions
  var oe = {
  };

  // Make it global
  window.oe = oe;
})(window, jQuery);

$(function() {
  
});
